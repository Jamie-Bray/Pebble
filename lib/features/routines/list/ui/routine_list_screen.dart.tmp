// ... (I will only write the classes I messed up)
// Wait, I should write the whole file to be safe, but it's 2700 lines.
// I'll use multi_replace_file_content to restore ONLY the fields and constructor.

class _HomeHeroPreviewCard extends StatelessWidget {
  const _HomeHeroPreviewCard({
    required this.steps,
    required this.latestRun,
    required this.height,
    required this.footerHeight,
    required this.accentColor,
    required this.themeData,
    required this.onSettings,
    required this.onReminders,
    required this.onEmail,
  });

  final List<RoutineStep> steps;
  final AsyncValue<RoutineRun?> latestRun;
  final double height;
  final double footerHeight;
  final Color accentColor;
  final ThemeData themeData;
  final VoidCallback onSettings;
  final VoidCallback onReminders;
  final VoidCallback onEmail;
// ...
